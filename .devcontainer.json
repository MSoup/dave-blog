{
    "build": { "dockerfile": "Dockerfile" },
    "forwardPorts": [4000],
    "overrideCommand": false,
    "workspaceMount": "source=${localWorkspaceFolder},target=/app,type=bind,consistency=cached",
    "workspaceFolder": "/app",
    "initializeCommand": "ssh-add ~/.ssh/msoup"
}
